<div class="card">
  <div class="card-content">
    <h2 class="card-title">{{title}}</h2>
  </div>
  <div class="card-body">
    <p class="card-text">{{post}}</p>
  </div>
  <div class="card-content">
    {{#if user.name}}
    <h3>By: {{user.name}}</h3>
    {{/if}}
  </div>
</div>